# Deprecation notice
#
# LangChain harness 已由 agent-pi（Pi coding agent）替代。
# 对话请走 agent-pi（默认 :8767）；本目录仅保留配置辅助与历史 CLI。
#
# 启动聊天服务：
#   cd ../agent-pi && npm run start
#
# CLI（仍可用，但依赖 LangChain，后续可能移除）：
#   python main.py -q "..."
