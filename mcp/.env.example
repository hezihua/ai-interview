# 生产配置请写在仓库根目录 .env（Agent 与 MCP 共用 MCP_AUTH_TOKEN）。

WORKSPACE_DIR=workspace

MCP_TRANSPORT=streamable-http
MCP_HOST=0.0.0.0
MCP_PORT=8765
MCP_PATH=/mcp
MCP_AUTH_TOKEN=change-me-to-a-long-random-string
MCP_ALLOWED_HOSTS=*
